apiVersion: v1
kind: Service
metadata:
  name: example-app-service-client
  labels:
    app: client

spec:
  selector:
    app: client
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 3000
